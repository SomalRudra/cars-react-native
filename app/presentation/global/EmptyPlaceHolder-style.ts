import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    parent: {
        height: '100%',
        width: '100%',
        alignItems: 'center',
        marginTop: 50
    },
    hintText: {
        fontSize: 15,
        marginHorizontal: 60,
        marginBottom: 20
    },
    image: {
        width: 300,
        height: 250,
        borderRadius: 10
    }
})

export default style;